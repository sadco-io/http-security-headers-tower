# Every PR auto-requests review from the owners listed here, and
# `main` is protected with "require review from Code Owners", so a
# matching owner's approval is required before merge.
#
# When adding path-specific entries below the wildcard, note that the
# LAST matching rule wins -- a later narrow rule replaces the owners
# from the wildcard for those paths rather than adding to them.

* @danielrcurtis

# The security posture of this crate lives in these files: the header
# policies themselves, the presets that most users adopt wholesale,
# and the nonce generation. Called out so the ownership is explicit if
# the wildcard above is ever narrowed.
/src/policy/ @danielrcurtis
/src/preset.rs @danielrcurtis

# Supply chain and CI. deny.toml governs which advisories and licenses
# are tolerated; the workflows are what enforce every other check.
/deny.toml @danielrcurtis
/.github/ @danielrcurtis
